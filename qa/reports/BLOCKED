SLICE A2a — piece 3 is being written right now; a `full` run would be
both wasted and false. Not a defect: work in flight.

Failing command: `tools/hearthstead-qa gate`
Error evidence:
  GATE: FAIL
   - STALE: source changed since last QA run

Why it is stale, precisely: the last green pair of full runs
(green_streak=2, fingerprint `20260824T222152Z`) predates A2a piece 4,
which added seven generated sounds and wired them into `CourierWorkGoal`.
That is real new source, so the marker is correct.

Why `full` is NOT the right next command *at this instant*: a
`sonnet-builder` agent is actively editing
`client/model/SettlerAnimations.java`, `client/model/SettlerModel.java`
and `tools/anim_check.py` (A2a piece 3, the five courier clips). This
repository has a hard, evidence-backed rule that source must not change
while a `full` run executes -- `playtest.sh` compares its jar against
every source mtime and reports a false "stale jar" when it does. Starting
`full` now would spend ~13 minutes to produce a result about a tree that
no longer exists by the time it finishes.

Verified state of the work that IS complete (not a claim, re-runnable):
  tools/hearthstead-qa fast  ->  PASS
    doctor PASS, assets PASS 466/466, animation PASS,
    build PASS, gametest PASS (31 tests), behavior PASS (0 findings)
  Sound determinism: generator re-run, oggs byte-identical.
  All of it committed and pushed to
  claude/hearthstead-settlement-mod-vbdb9n.

Completed since the last green gate:
  - A2a piece 4: 7 courier sounds (haul_step x3, crate_grip, haul_strain,
    crate_creak, crate_down, item_pickup, chest_stow), generated by the
    deterministic pipeline, registered, subtitled in both languages, and
    wired into CourierWorkGoal's real trigger points with named public
    tick constants so piece 3's clips bind to the same contract.

Exact resume action:
  1. Wait for the piece-3 agent's completion notification (do not poll it,
     do not spawn a duplicate, do not edit its three owned files).
  2. Verify its report: `python3 hearthstead-neoforge/tools/anim_check.py`
     and `cd hearthstead-neoforge && ./gradlew compileJava`.
  3. `tools/hearthstead-qa fast` (~50s) for the whole tree.
  4. THEN, with the tree frozen and nobody editing:
     `tools/hearthstead-qa full` twice back to back, hands off the tree
     between and during them, until `gate` reports green_streak >= 2.
  5. Capture a live film of a courier round trip (lift, laden walk,
     set-down, sort) as A2a's runtime evidence.

This file is deleted automatically by the next `full` run.
