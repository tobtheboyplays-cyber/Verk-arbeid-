BLOCKED: the playtest suite's simulated-input step is intermittent in-suite,
and the owner's 18:00 demo window is now closed for further green_streak
attempts (this written 17:52).

Failing command: tools/hearthstead-qa full
  -> [playtest] FAIL at expect_server:hearthstead:build_plan
     (the plaque-insert step of qa/scenarios/default.txt)

Score for that one step since all known causes were fixed today:
  PASS 16:01 (standalone), 16:30 (standalone), 16:45-17:01 (inside full,
       manifest 20260826T144511Z, overall PASS, streak 1),
       17:18-17:34 (inside full, manifest 20260826T151818Z, overall PASS,
       streak 1)
  FAIL 16:16, 17:16, 17:49 (inside full; same assertion; aim proven
       dead-centre by plaque-01-before-click.png each time)
Every other suite -- gametest (266), behavior, dedicated incl. restart
persistence, performance, client, assets, animation, build, doctor --
passed in EVERY full run today, seven consecutive times.

The mechanic the flaky step exercises is deterministically proven by
PlaqueUseGameTests (right-click fits the plan, consumes it in survival,
refuses a non-plan item; no window, no input layer). The intermittency is in
the xdotool/GLFW/Xvfb simulated-input path, not in the product.

Three real causes were found, fixed and screenshot-proven today: the wrong
hotbar slot that opened the pause menu; the unverified click primitive vs a
dead GLFW grab; and the integer-teleport block-seam aim ambiguity. A fourth
intermittency remains undiagnosed.

Exact resume action:
  tools/hearthstead-qa full   # twice, unattended, at this fingerprint
  # On a red, pull shots/plaque-01-before-click.png and film/ from the
  # newest qa/reports/artifacts/playtest/ dir FIRST -- every diagnosis today
  # that started from logs was wrong; every one that started from the
  # screenshot was right. Next suspect: the `open` directive's safe_regrab
  # click landing while the plan is already in hand (a right-click on AIR at
  # the wrong moment may desync the subsequent insert click's timing);
  # instrument by capturing a shot immediately after safe_regrab inside
  # `open` before theorising further.
