NOT READY — PLAQUE-1's code is done; one playtest interaction is harness-blocked

Written 2026-08-24. Deleted automatically by the next full run, so it cannot
outlive what it describes. This is NOT READY, never PASS.

WHY THE GATE IS RED
  `playtest` fails two `expect_server` checks in the PLAQUE-1 scenario
  section. This is KF-009 (docs/project/KNOWN_FAILURES.md) — extensively
  diagnosed, NOT a mod defect. Full evidence trail there, summary:

  The mod is proven correct twice over, independently of this failure:
    1. GameTest (deterministic, no client input) — green, 19/19, twice at
       two different fingerprints.
    2. Manual reproduction with the real client, same click mechanism the
       scenario uses — the plaque genuinely reached State: "linked_valid"
       and stayed there, with the actual PlaqueScreen UI on screen showing
       "Registered" and an assigned resident. Evidence:
       qa/reports/artifacts/live/20260824T083253Z/.

  What fails is the SCRIPTED playtest scenario's own click/chat-command
  delivery, intermittently, five-plus real minutes into a long run — the
  same unresolved class of flakiness already recorded in KF-006 for
  move/look ("exact cause not fully isolated"), now confirmed to also
  affect click and cmd. Two targeted, genuine fixes were applied and BOTH
  verified NOT sufficient (kept anyway, both are real hardening):
  belt-and-braces double-click, and a self-healing focus() that no longer
  silently swallows a stale-window failure. KF-009 lists what was ruled out
  and what remains unexplored for whoever picks this up next.

WHAT ELSE IS GREEN AT THE CURRENT FINGERPRINT
  assets (230/230, closes KF-004 and KF-005) and gametest (19/19, closes
  KF-001 — room-detection fix + save-compat via a synthetic legacy tag).

EXACT RESUME ACTION
  Do not re-litigate settlement distance, aim precision, or the two fixes
  above (all evidenced closed or evidenced insufficient in KF-009). Either:
  (a) invest further in the harness input-delivery mystery per KF-009's
  "promising unexplored directions", or (b) accept this as a documented
  environmental blocker, send PLAQUE-1 to its RELEASE_GATE (one Opus call,
  still available — 0 of 3 spent on this slice) for an independent judgement
  on whether GameTest + manual reproduction is sufficient proof to call the
  slice done with this one item BLOCKED, and move to VISUAL-1.

Branch claude/hearthstead-settlement-mod-vbdb9n, all work committed and pushed.
